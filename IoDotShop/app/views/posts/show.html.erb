<!DOCTYPE html>
<html>

  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"%>

  <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-0 mb-3 border">
      <h2><%= @post.title %></h2>
      <div class="container">
        <header class=" d-flex flex-wrap justify-content-center py-3 mb-0 border-top">
          <p><h5><%= @post.body %></h5></p>
        </header>
      </div>
    </header>
  </div>
</html>